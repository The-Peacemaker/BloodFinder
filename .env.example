# Environment Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (Replace with your MongoDB connection string)
MONGODB_URI=mongodb://localhost:27017/bloodfinder

# JWT Configuration (Replace with your own secure secret)
JWT_SECRET=your_super_secure_secret_key_here

# CORS Configuration (Replace with your domain)
FRONTEND_URL=http://localhost:3000